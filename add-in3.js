/* Add at least 3 dynamic components to your web page. 
These can be based on user input, or other dynamic input data. 

At least one dynamic component should be based on user input.
*/

var userPrompt = prompt("Are you a Looter? (y/n):")

if (userPrompt == 'y') {
    document.write('<img src="images/american-flag.jpg" width="100"/>');
} else continue;